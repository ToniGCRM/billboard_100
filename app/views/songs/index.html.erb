<h1>Songs</h1>

<% @songs.each do |song| %>
  <%= @songs.name %>

  <%= link_to 'New', new_song_path %>
  <%= link_to 'Show', song_path(@songs) %>
  <%= link_to 'Edit', edit_song_path(@songs) %>

  <% end %>