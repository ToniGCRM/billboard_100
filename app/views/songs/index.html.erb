<h1>Songs</h1>
<br/>
<ul>

<% @songs.each do |song| %>
  <li>
  <%= link_to song.name, artist_song_path(@artist, song) %>
  </li>
  <% end %>
  </ul>
  <br/>
  <div id="link_container">
  <%= link_to 'New', new_artist_song_path(@artist) %>
  <%= link_to 'Artist', billboard_artist_path(@artist) %>


  </div>



